/**
 * Navigation content — which is just the mark and the action.
 *
 * A waitlist page has one destination, so it has no navigation. The header
 * carried two three-column mega-menus whose eighteen links all pointed at two
 * anchors that no longer exist; they are gone, and `Navbar` is the bar you see.
 */

export const wordmark = 'Thinq'

/** Alt text for the logo lockup. */
export const wordmarkAlt = 'Thinq home'

export const signupLabel = 'Join the waitlist'
