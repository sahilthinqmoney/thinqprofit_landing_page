# Pre-launch. The page behind this carries unfilled compliance placeholders and
# a waitlist form that stores nothing — see docs/go-live-checklist.md. Nothing
# here should be indexed until that list is closed.
#
# This file is the polite half of the gate; middleware.ts is the half that does
# not depend on a crawler's cooperation. Delete both at launch and replace this
# with a real robots.txt and a sitemap.
User-agent: *
Disallow: /
