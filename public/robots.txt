# Pre-launch. The page behind this carries unfilled compliance placeholders and
# a waitlist form that stores nothing — see docs/go-live-checklist.md.
#
# The deployment is publicly reachable by anyone with the URL; this file and the
# `noindex` meta in index.html are the only thing keeping the draft out of search
# results, and they work only for crawlers that choose to honour them.
#
# Replace with a real robots.txt and a sitemap at launch.
User-agent: *
Disallow: /
