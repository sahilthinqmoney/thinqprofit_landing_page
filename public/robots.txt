# Thinq — https://thinq.co/
#
# This replaced a pre-launch `Disallow: /`. Nothing is disallowed now: the two
# public routes are the homepage and /terms, and both are meant to be found.
#
# Nothing under /api/ is listed either. Those endpoints are POST-only or require
# a session, they are never linked from a page, and a Disallow line would do
# nothing but publish the paths to anyone who reads this file.
#
# CSS, JavaScript, images and video are deliberately left crawlable — blocking
# them stops a crawler rendering the page it is trying to judge.

User-agent: *
Allow: /

Sitemap: https://thinq.co/sitemap.xml
